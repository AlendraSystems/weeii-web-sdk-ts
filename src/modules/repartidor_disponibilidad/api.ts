/**
 * RepartidorDisponibilidad domain APIs — stub.
 *
 * No client-callable endpoints are defined yet.
 * Server pushes `repartidor_disponibilidad` records; the store handles them automatically.
 */
